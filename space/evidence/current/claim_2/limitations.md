# Limitations and deviations

The existence result does not provide a practically tight context constant.
The certificate proves finiteness for each fixed layer; it does not claim a
uniform constant over all matrices or steps.
