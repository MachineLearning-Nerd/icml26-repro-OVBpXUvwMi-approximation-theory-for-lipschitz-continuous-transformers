# Claim 2 source audit

- Anchor: Lemma `lem:property-in-context-map-1`, source lines 256–266; proof
  lines 872–1111.
- Query quantifier: each fixed `mu`, all query pairs in compact `Omega`.
- Measure quantifier: each fixed `x`, all `mu,nu` in `P(Omega)`.
- The proof makes explicit that the measure constant depends on fixed `A`,
  `eta`, and `Omega`; “depending on Omega” in the short statement must not be
  read as uniform over unbounded choices of `A`.
