# Claim 1 — VERIFIED

This is a dimension-free symbolic derivation with a machine-checked dependency
certificate. The high-dimensional exhaustive mask run is an independent
corroboration, not the proof. The verifier exits nonzero if the proof DAG,
independent checker, baseline regression, or negative control fails.
